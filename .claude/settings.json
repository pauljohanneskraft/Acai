{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.swift) swift format --in-place --configuration .swift-format \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Formatting Swift"
          }
        ]
      }
    ]
  }
}
